<Page
    x:Class="GameSpaceManager.Presentation.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:entities="clr-namespace:DataAccessLayer.Entities;assembly=DataAccessLayer"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <ScrollViewer>
        <StackPanel Padding="24" Spacing="16">

            <!-- Page Title -->
            <TextBlock Text="Settings"
                       FontSize="32"
                       FontWeight="Bold"
                       Foreground="{ThemeResource PrimaryColor}" />

            <!-- Source folders -->
            <TextBlock Text="Source Folders" FontSize="22" FontWeight="SemiBold" Foreground="{ThemeResource PrimaryColor}" Margin="0,16,0,4" />
            <ItemsControl x:Name="SourceFoldersList"
                          ItemsSource="{x:Bind _sourceFolders, Mode=OneWay}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate x:DataType="entities:SourceFolderEntity">
                        <!-- Outer border for separator -->
                        <Border BorderBrush="{ThemeResource SystemControlForegroundBaseLowBrush}"
                                BorderThickness="0,0,0,1">
                            <Border Padding="8"
                                    CornerRadius="4">
                                <Grid ColumnSpacing="12">
                                    <!-- Define columns: text expands, button auto-sized -->
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>

                                    <!-- Folder path -->
                                    <TextBlock Grid.Column="0"
                                               Text="{x:Bind Path}"
                                               VerticalAlignment="Center"
                                               TextWrapping="Wrap" />

                                    <!-- Delete pill button -->
                                    <Button Grid.Column="1"
                                            Click="RemoveSourceFolder_Click"
                                            Tag="{x:Bind}"
                                            Padding="8,4"
                                            Background="{ThemeResource SecondaryColor}"
                                            BorderThickness="0"
                                            CornerRadius="16"
                                            HorizontalAlignment="Right">
                                        <StackPanel Orientation="Horizontal" Spacing="4">
                                            <SymbolIcon Symbol="Delete" />
                                            <TextBlock Text="Remove" VerticalAlignment="Center" />
                                        </StackPanel>
                                    </Button>
                                </Grid>
                            </Border>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <Button Content="Add Source Folder"
                    Click="AddSourceFolder_Click" Margin="0,4,0,0" />

            <!-- Destination folder -->
            <TextBlock Text="Destination Folder" FontSize="22" FontWeight="SemiBold" Foreground="{ThemeResource PrimaryColor}" Margin="0,16,0,4" />
            <StackPanel Orientation="Horizontal" Spacing="8">
                <TextBox x:Name="DestinationFolderTextBox" Width="400" IsReadOnly="True" />
                <Button Content="Browse" Click="BrowseDestination_Click" />
            </StackPanel>

        </StackPanel>
    </ScrollViewer>
</Page>